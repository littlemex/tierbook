{
  "config_format": 1,
  "_comment": "A worked example of the candidate file, matching the example ledger. Every value here is an unverified claim by whoever wrote it, which is exactly what a candidate file holds. What was observed lives in tiers/ and only that can route.",
  "candidates": {
    "api-strong-a": {
      "deployment": "api",
      "endpoint": {
        "base_url": "https://gateway.example/v1",
        "model": "strong-model-a",
        "wire": "chat",
        "api_key_env": "TIERBOOK_API_KEY",
        "revision": "2026-07"
      },
      "price_per_mtok": {
        "fresh_in": 10.0,
        "cached_in": 1.0,
        "out": 50.0
      },
      "note": "the reference for both families in the example ledger"
    },
    "api-cheap-a": {
      "deployment": "api",
      "endpoint": {
        "base_url": "https://gateway.example/v1",
        "model": "cheap-model-a",
        "wire": "responses",
        "api_key_env": "TIERBOOK_API_KEY",
        "revision": "2026-08",
        "refuses_params": [
          "temperature"
        ]
      },
      "price_per_mtok": {
        "fresh_in": 2.2,
        "cached_in": 0.22,
        "out": 13.2
      },
      "note": "wire is 'responses' because this endpoint refuses function tools with a reasoning setting on chat completions; the score belongs to the wire it was measured over"
    },
    "self-hosted-a": {
      "deployment": "self_hosted",
      "endpoint": {
        "base_url": "http://model-service.example:8000/v1",
        "model": "open-weights-b"
      },
      "hourly_usd": 15.2174,
      "note": "an hourly bill, so its cost per request depends on this family's realised throughput and cannot be borrowed from another family's"
    }
  },
  "families": {
    "agentic-coding": "api-strong-a",
    "tool-agent-user-retail": "api-strong-a"
  },
  "objective": {
    "objective": "cost",
    "constraints": {
      "non_inferiority": {
        "margin": 0.25
      }
    }
  },
  "_throughput_note": "No throughput override here on purpose. Each record carries its own per-family latency and the concurrency it was measured at, so the figure lives in one place. Override only when you measured throughput under load -- an assumed concurrency is what made an earlier figure in this project wrong."
}
